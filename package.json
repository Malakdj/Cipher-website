{
  "name": "cipher-website",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "install-gcc": "apt-get update && apt-get install -y --no-install-recommends g++",
    "postinstall": "npm run install-gcc && npm install express"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": "18.x"
  }
}